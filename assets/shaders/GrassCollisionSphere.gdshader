shader_type spatial;
render_mode unshaded; // Чтобы не тратить ресурсы на свет

void fragment() {
    // В Godot 4 в переменной PROJECTION_MATRIX[3][3] 
    // у Перспективной камеры всегда 0.0, а у Ортогональной 1.0.
    // Камера плагина — Ортогональная (смотрит сверху).
    
    if (PROJECTION_MATRIX[3][3] == 0.0) {
        discard; // Полностью удаляем пиксель из отрисовки в окне редактора
    }
    
    ALBEDO = vec3(1.0, 1.0, 1.0); // Цвет для маски плагина (белый)
}